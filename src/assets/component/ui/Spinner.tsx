import { PropagateLoader } from "react-spinners"


const Spinner = () => {
    return (
        <section className="sectionPage flex items-center justify-center">
            <PropagateLoader color="#00b6ff" />
        </section>
    )
}

export default Spinner